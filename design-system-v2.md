# design-system-v2.md — залогиненная часть energydess.ru

> **Единственный источник правды по визуалу залогиненной части**
> (инструменты, профиль, админка) с 2026-09-19, BACKLOG №352.
> Прежний `design-system.md` помечен как заменённый и остаётся
> справочником по коду, который ещё не переведён: переход идёт письмами
> 2–8 плана, до конца перехода старые токены живут рядом с новыми.
> Гостевая главная (`landing.css`, токены `--landing-*`) этим документом
> не описывается и не меняется.

Значения объявлены в ОДНОМ месте — `static/v2.css`, блок `:root`,
пространство имён `--v2-*`. Компоненты ниже `:root` используют только
`var(--v2-*)`; это сторожит проверка 47 (`py check_v2_tokens.py`).
Живые образцы всех компонентов — витрина `/admin/design-v2`
(только администратору).

## Оглавление

1. Основа: фон, поверхности, рамки, звёзды и свечение
2. Шрифты
3. Отступы и скругления
4. Цвета инструментов
5. Компоненты
6. Каркас
7. Телефон
8. Анимации
9. Соответствие «старое → новое»
10. Решения, которые не пересматриваются

---

## 1. Основа

### 1.1. Направление

Гибрид: оболочка и шапки страниц говорят языком гостевой главной
(Unbounded, тонкие светлые рамки, таблетки, звёзды и свечение), рабочая
область спокойная и плотная. Тёмно-синих плашек нет: поверхность почти
прозрачна, от фона её отделяет тонкая светлая рамка.

### 1.2. Фон и поверхности

| токен | значение | назначение |
|---|---|---|
| `--v2-bg` | `#0a0b12` | фон страницы; верх неба гостевой главной |
| `--v2-surface-1` | белый 2 % | главная карточка |
| `--v2-surface-2` | белый 4 % | вложенное окно, плитка |
| `--v2-surface-field` | белый 3 % | поле ввода |
| `--v2-surface-hover` | белый 6 % | наведение, выбранный пункт |
| `--v2-surface-pop` | `#16171e` | меню, панель ассистента — непрозрачно |
| `--v2-scrim` | тёмный 72 % | подложка под окном |

Меню и панель непрозрачны намеренно: полупрозрачное меню поверх текста
страницы нечитаемо.

### 1.3. Рамки трёх уровней

Стартовые прозрачности письма (.12–.14, .08–.10, .06–.08) уточнены
замером контраста белого под альфой к фону `--v2-bg`, к главной
карточке и к вложенному окну:

| альфа | к фону | к карточке | к вложенному |
|---:|---:|---:|---:|
| .06 | 1.13 | 1.14 | 1.16 |
| .07 | 1.16 | 1.18 | 1.19 |
| .08 | 1.19 | 1.21 | 1.24 |
| .10 | 1.25 | 1.29 | 1.31 |
| .12 | 1.34 | 1.38 | 1.40 |
| .14 | 1.43 | 1.47 | 1.51 |
| .24 | 2.09 | 2.12 | 2.16 |

| уровень | токен | альфа | почему это значение |
|---|---|---:|---|
| главные карточки и поля | `--v2-line-1` | .14 | утверждено на гостевой главной (рамка карточки инструментов) |
| вложенные окна | `--v2-line-2` | .10 | утверждено там же (рамка окна внутри карточки); ниже `--v2-line-1`, чтобы вложенное не читалось главнее объемлющего |
| разделители | `--v2-line-3` | .07 | середина диапазона письма; разделитель не спорит с рамкой |
| наведение | `--v2-line-hover` | .24 | 2.09 к фону — заметная смена в покое |
| граница, обязанная дать 3.0 | `--v2-line-strong` | .36 | первое значение ≥ 3.0 на всех поверхностях (прежний `--border-contrast`) |

ИТОГ НАЗВАН ЧЕСТНО: рамки карточек декоративны и под WCAG 1.4.11 не
подпадают. Граница ПОЛЯ ВВОДА в покое даёт 1.47 — поле опознаётся ещё
заливкой и подписью, а в фокусе рамку заменяет обводка цвета инструмента
(≥ 5.96). Где граница несёт смысл сама, берётся `--v2-line-strong`.

### 1.4. Звёзды и свечение — только в шапке страницы

Шапка страницы (`.v2-page-head`) — единственное место с декором:
звёзды — тот же рецепт, что у гостевой главной (тайл радиальных
градиентов, без файла, `--v2-star` белый 42 %, `--v2-star-dim` 30 %).
В рабочей области звёзд и свечения нет.

**СВЕЧЕНИЕ ЛИНЕЙНОЕ, КРУГОВ В НЁМ НЕТ НИ ОДНОГО** (решение владельца
2026-09-20). Заливка `--v2-tool-glow` идёт во всю ширину шапки: у левого
края заметнее, к правому гаснет до нуля, сверху вниз — в фон страницы,
нижняя граница не видна. Лежит в `.v2-page-head::after` (слой −2, под
звёздами), маска составная: вертикальная гасит к низу, горизонтальная —
вправо, `mask-composite: intersect`.

**ЗЕРНО ПОВЕРХ ГРАДИЕНТА ОБЯЗАТЕЛЬНО** — `static/v2-grain.svg`, статичная
плитка 200×200, без анимации и без CSS `filter`. На тёмном фоне линейный
градиент ложится широкими полосами одного цвета; замер 2026-09-20, самый
длинный такой участок в строке шапки на 1920:

| инструмент | круглое свечение | линейное с зерном |
|---|---:|---:|
| HH-ассистент | 973 | **382** |
| дневник | 1003 | **403** |
| тренировки | 577 | **231** |
| Enshrouded | 933 | **384** |
| аптечка | 954 | **382** |

Окрашенных пикселей в той же строке стало БОЛЬШЕ (155–290 → 613–1050):
заливка занимает всю ширину, а не левую четверть.

Рамки у шапки НЕТ (решение владельца 2026-09-19): звёзды гаснут маской
к низу, свечение уходит в прозрачность, и шапка переходит в фон страницы
без границы. Снизу её отделяет только линия под вкладками. Проверяется
пикселем: на 20 px ниже линии вкладок цвет совпадает с фоном страницы
(отклонение 0 по всем пяти инструментам).

### 1.4a. Одна ширина содержимого

`--v2-content` (1600 px) — ширина колонки содержимого, ОДНА на все
вкладки и все инструменты (решение владельца 2026-09-20). Контейнер
у каждого инструмента свой (`main.container` у HH и аптечки,
`.v2-page-wrap` у дневника, `main.wk-page` и `main.wkp-page`
у тренировок, `.ens-headwrap`/`.ens-bar`/`main.ens-main` у Enshrouded),
и все они перечислены в одном правиле `v2.css`; полноту перечня сторожит
`py check_v2_head.py --ширина`.

`width: 100%` там обязателен: колонка каркаса — флексбокс, и `margin:
auto` по бокам отменяет растяжение. Замер 2026-09-20: у HH контейнер
выходил 1100 px при объявленных 1600, колонки письма были по 530 px;
стало 1600 и 748.

### 1.5. Текст

| токен | значение | к фону | к карточке | к вложенному | к меню |
|---|---|---:|---:|---:|---:|
| `--v2-text` | `#E8ECF8` | 16.63 | 16.07 | 15.43 | 15.13 |
| `--v2-text-2` | `#AEB8D4` | 9.92 | 9.58 | 9.20 | 9.02 |
| `--v2-text-faint` | `#7A83A0` | 5.22 | 5.04 | 4.84 | 4.75 |
| `--v2-text-strong` | `#FFFFFF` | — | — | — | — |

Вторичный текст — один токен `--v2-text-2`; `--v2-text-faint` проходит
4.5 на всех поверхностях. Состояния: `--v2-ok`, `--v2-warn`,
`--v2-danger`, `--v2-danger-dim` — прежние значения.


## 1.4b. Резюме по разделам: вид поверх текста

**Правило: резюме хранится и уходит в генерацию писем ПОЛНЫМ текстом.
Разделы — способ его показать и поправить по частям, и на то, что видит
модель, они не влияют ни одним знаком.**

Решение владельца 2026-09-20 (задача 352, письмо 3, блок 3). Разбирает
текст СЕРВЕР (`resume_sections.py`), рисует — единственный построитель
`templates/_hh_resume.html`; он же отдаётся после правки раздела, поэтому
второго построителя в браузере нет (§6.0.15).

| Что | Где | Почему так |
|---|---|---|
| «Опыт работы» карточками мест | левая колонка | самая длинная часть резюме: в одной колонке отжимала бы всё прочее за экран |
| остальные разделы | правая колонка | «Контакты» первым — это верхний блок выгрузки до первого заголовка |
| описание места | свёрнуто до трёх строк, «ещё» | кнопка прячется там, где текст влез: орган, ничего не разворачивающий, обещает действие, которого нет |
| «Ключевые навыки» | чипы `.v2-chip` | ТОЛЬКО строка, где hh.ru перечислил их набегами пробелов — остальное текстом |
| правка | «Изменить» → поле с ДОСЛОВНЫМ телом → «Сохранить раздел» / «Отмена» | полный текст собирает сервер: ошибка сборки в браузере стоила бы всего резюме, а не одного раздела |
| заголовков нет вовсе | текст целиком одним полем и строка о причине | «разделов нет» и «резюме пустое» — разные факты |

**НА ЭКРАНЕ СТРОКА ЗАГОЛОВКА СРЕЗАНА, В ТЕЛЕ — НЕТ.** Имя раздела стоит
шапкой карточки, и печатать его второй раз строкой ниже значит сказать
одно и то же дважды (замер на выгрузке hh.ru: так выглядели семь разделов
из восьми). Срезается только ПОКАЗ; тело остаётся дословным, иначе
побайтность держалась бы на честном слове. У заголовка с хвостом («Опыт
работы — 11 лет 4 месяца») срезается только слово заголовка: хвост — это
данные.

**ПУСТОЙ РАЗДЕЛ ГОВОРИТ ЭТО СЛОВОМ** («В этом разделе резюме ничего
нет»), а не показывает пустую карточку: пустота неотличима от потерянного
текста.

## 2. Шрифты

| семейство | токен | где |
|---|---|---|
| Unbounded | `--v2-font-display` | заголовки страницы и карточек, цифры плиток |
| Manrope | `--v2-font` | весь текст, кнопки, поля |
| JetBrains Mono | `--v2-font-mono` | счётчики на чипах, технические значения |

Unbounded объявлен в `static/v2.css` своими `@font-face` (ось 200–900,
те же файлы, что у гостевой главной); `landing.css` этот файл
не подключает.

Одна шкала размеров на весь сайт:

| токен | значение | px (корень 16) | роль |
|---|---|---|---|
| `--v2-fs-xs` | 0.8125rem | 13 | абсолютный минимум: метки, подсказки |
| `--v2-fs-sm` | 0.9375rem | 15 | навигация, кнопки, чипы |
| `--v2-fs-md` | 1rem | 16 | текст |
| `--v2-fs-lg` | 1.125rem | 18 | заголовок карточки |
| `--v2-fs-xl` | clamp(1.25rem, 1.4vw, 1.5rem) | 20–24 | заголовок блока |
| `--v2-fs-2xl` | clamp(1.5rem, 2.2vw, 2rem) | 24–32 | цифра плитки |
| `--v2-fs-3xl` | clamp(1.75rem, 3vw, 2.75rem) | 28–44 | заголовок страницы |

Межстрочные: `--v2-lh-tight` 1.1 (Unbounded), `--v2-lh-snug` 1.3
(кнопки, заголовки карточек), `--v2-lh-body` 1.55 (текст).
Разрядка: `--v2-ls-display` −0.02em у заголовков, `--v2-ls-label`
0.08em у меток прописными. Начертания: `--v2-fw-text` 400,
`--v2-fw-medium` 500, `--v2-fw-strong` 600, `--v2-fw-num` 700,
`--v2-fw-display` 800 (заголовок страницы — как на гостевой главной).
Тоньше 400 не бывает.

## 3. Отступы и скругления

Одна шкала, кратная 4: `--v2-sp-1` 4 · `--v2-sp-2` 8 · `--v2-sp-3` 12 ·
`--v2-sp-4` 16 · `--v2-sp-5` 24 · `--v2-sp-6` 32 · `--v2-sp-7` 48 ·
`--v2-sp-8` 64 · `--v2-sp-9` 80.

Скругления: `--v2-r-sm` 8 · `--v2-r-md` 12 (поля) · `--v2-r-lg` 16
(вложенные окна, меню, плитки) · `--v2-r-xl` 24 (главные карточки,
шапка страницы, панель) · `--v2-r-pill` 999 (кнопки, чипы, вкладки
сегментов).

Тени: одна — `--v2-shadow-pop` у всплывающего меню. Карточки теней
не имеют: их отделяет рамка.

## 4. Цвета инструментов

### 4.1. Значения

| инструмент | токен | значение | к фону | к карточке |
|---|---|---|---:|---:|
| HH-ассистент | `--v2-hh` | `#4F8FFF` | 6.27 | 6.06 |
| Дневник питания | `--v2-nutrition` | `#10B981` | 7.74 | 7.48 |
| Аптечка | `--v2-medkit` | `#06B6D4` | 8.09 | 7.81 |
| Enshrouded | `--v2-enshrouded` | `#D97706` | 6.16 | 5.96 |
| Тренировки | `--v2-workout` | `#D08CFF` | 8.30 | 8.02 |

Все пять проходят 4.5 к фону и к карточке как текст — значит годятся
для метки шапки и иконки.

**ТРЕНИРОВКИ — ФИОЛЕТОВЫЙ (решение владельца 2026-09-19, письмо 2).**
Прежний `#F59E0B` отличался от Enshrouded на ΔE2000 **12.2** — пара
читалась одним цветом. `#D08CFF`: оттенок 275°, тёмная надпись 8.30,
к фону 8.30 (не хуже остальных четырёх: 6.16–8.09). Различимость —
CIEDE2000 для всех пар (расчёт тем же скриптом, что контраст):

| пара | ΔE2000 |
|---|---:|
| hh — medkit | **23.5** (минимальная) |
| hh — workout | 24.6 |
| nutrition — medkit | 28.8 |
| medkit — workout | 38.1 |
| hh — nutrition | 45.4 |
| nutrition — workout | 48.3 |
| medkit — enshrouded | 49.6 |
| nutrition — enshrouded | 50.7 |
| enshrouded — workout | 52.1 |
| hh — enshrouded | 52.7 |

Минимальная пара теперь hh — medkit, и она прежняя: фиолетовый её
не ухудшил. Enshrouded остаётся оранжевым.

Цвет ставится классом `.v2-tool-hh` … `.v2-tool-workout` (на `<body>`
с письма 2), класс объявляет `--v2-tool`; производные `--v2-tool-dim`
(16 %), `--v2-tool-line` (55 %) и `--v2-tool-glow` (22 %) выводятся
через `color-mix` и своих значений не имеют.

### 4.2. Где цвет инструмента применяется — и нигде больше

Иконка инструмента, метка в шапке, активная вкладка, главная кнопка,
прогресс, свечение шапки. Плюс производные в тех же ролях: фон
активного чипа, пузырь своего сообщения в панели ассистента, кружок
пустого состояния.

Запрещён: в тексте абзацев, рамках карточек, заголовках, поверхностях,
вторичных кнопках, пунктах меню кроме иконки.

### 4.3. Надпись на таблетке — ПРЕДЛОЖЕНИЕ, отмечено отдельно

Белая надпись на заливке цвета инструмента НЕ ПРОХОДИТ 4.5 ни у одного:

| инструмент | белая | тёмная `--v2-bg` |
|---|---:|---:|
| hh | 3.13 | 6.27 |
| nutrition | 2.54 | 7.74 |
| medkit | 2.43 | 8.09 |
| enshrouded | 3.19 | 6.16 |
| workout | 2.37 | 8.30 |

Меняется надпись: `--v2-ink-on-tool` — тёмная у всех пяти
(6.16–8.30). **Утверждено владельцем 2026-09-19.**

### 4.4. Градиентная таблетка

Только «Регистрация» и «Связаться». Точки — `--v2-grad-a` `#146AFF`,
`--v2-grad-b` `#8257A2` (смесь крайних пополам), `--v2-grad-c`
`#A25904`, те же, что у гостевой главной; белая надпись 4.63 · 5.48 ·
5.29.

## 5. Компоненты

Классы — `static/v2.css`, образцы — витрина. Везде: высота органа
40 px (`--v2-ctl-h`), на сенсорном 44 (`--v2-ctl-h-touch`); наведение
только внутри `@media (hover: hover)`; фокус — §5.1.

### 5.1. Фокус и загрузка

**ФОКУС — только с клавиатуры** (`:focus-visible`): при клике мышью кольца
нет. Кольцо 2 px ВПЛОТНУЮ к органу (`outline-offset: 0`, у вкладок и пунктов
меню — внутрь), без белого и без тёмного зазора: прежнее белое кольцо
с отступом 2 px читалось второй обводкой. Цвет — цвет самого органа,
переменная `--v2-ring`:

| орган | кольцо | к фону |
|---|---|---:|
| главная | `--v2-tool` | 6.16–8.30 |
| опасная | `--v2-danger` | 5.22 |
| градиентная | `--v2-grad-b` (середина градиента) | 3.58 |
| вторичная, вкладка, чип, сегмент, пункт меню | `--v2-ring-neutral` | 9.92 |

**ЗАГРУЗКА — одно правило на все виды.** Кольцо размером со значок слева
от текста, группа по центру, зазор `--v2-sp-2`; ширина кнопки НЕ меняется.
У кнопки со значком кольцо встаёт на место значка, у кнопки без значка
поля уже на половину «кольцо + зазор».

**ГРАДИЕНТНАЯ КНОПКА — `background-origin: border-box`.** Рамка прозрачна,
а фон по умолчанию отсчитывается от края поля и повторяется под рамкой:
у левого края был виден конец градиента, у правого — начало.


| компонент | класс | правило |
|---|---|---|
| состояния на витрине | `.is-hover`, `.is-focus`, `.is-pressed` | классы-зеркала псевдоклассов для статичного образца; живые страницы их не ставят; образцы вынуты из порядка Tab (`tabindex="-1"`) |
| кнопка | `.v2-btn` | таблетка, рамка `--v2-line-1`; наведение — рамка `--v2-line-hover` и фон `--v2-surface-hover`; нажатие `scale(.98)`; неактивна — 45 %; загрузка — `.is-loading` вместе с `disabled`, кольцо вместо значка, без приглушения |
| главная | `.v2-btn-primary` | заливка цвета инструмента, надпись `--v2-ink-on-tool`; наведение — цвет светлеет на 14 % к белому |
| вторичная | `.v2-btn-secondary` | контурная, без заливки |
| опасная | `.v2-btn-danger` | контур и надпись `--v2-danger`, наведение — `--v2-danger-dim` |
| градиентная | `.v2-btn-gradient` | §4.4; наведение `scale(1.02)` |
| кнопка-значок | `.v2-btn-icon` | квадрат высоты органа |
| поле | `.v2-field`, `.v2-label`, `.v2-input`, `.v2-textarea`, `.v2-select`, `.v2-hint`, `.v2-error` | метка прописными 13 px; ошибка — `aria-invalid="true"`, рамка `--v2-danger`, текст `.v2-error` |
| сегменты | `.v2-seg`, `.v2-seg-btn` | таблетка в таблетке; выбранный — `--v2-surface-hover` |
| вкладки | `.v2-tabs`, `.v2-tab` | подчёркивание 2 px цвета инструмента у активной; прокрутка вбок без полосы |
| чипы | `.v2-chips`, `.v2-chip`, `.v2-chip-count` | активный — фон `--v2-tool-dim`, рамка `--v2-tool-line`; счётчик моноширинный |
| карточка 1 | `.v2-card` | `--v2-surface-1`, `--v2-line-1`, `--v2-r-xl`, поле 24 (16 на телефоне) |
| карточка 2 | `.v2-card-nested` | `--v2-surface-2`, `--v2-line-2`, `--v2-r-lg`, поле 16 |
| разделитель 3 | `.v2-divider` | `--v2-line-3` |
| плитка | `.v2-tile`, `.v2-tile-num`, `.v2-tile-label` | цифра Unbounded 700, табличные цифры |
| прогресс | `.v2-progress`, `.v2-progress-fill` | 6 px, доля — переменная `--v2-value` на заливке |
| пустое состояние | `.v2-empty`, `.v2-empty-icon`, `.v2-empty-title`, `.v2-empty-text` | кружок 64 px в цвете инструмента |
| меню | `.v2-menu`, `.v2-menu-item`, `.v2-menu-name`, `.v2-menu-sub`, `.v2-menu-sep` | непрозрачное, тень `--v2-shadow-pop`; пункт 44 px, иконка в цвете инструмента |
| шапка страницы | `.v2-page-head`, `.v2-head-text`, `.v2-head-label`, `.v2-head-title`, `.v2-head-desc`, `.v2-head-actions` | БЕЗ РАМКИ И СКРУГЛЕНИЯ; метка прописными в цвете инструмента, заголовок Unbounded 800, одна строка описания, справа кнопки (ассистент — только где он есть), ниже вкладки; звёзды и свечение растворяются в фоне к низу шапки, снизу — только линия под вкладками `--v2-line-2` (§1.4) |
| панель ассистента | `.v2-assist`, `.v2-assist-head`, `.v2-assist-title`, `.v2-assist-body`, `.v2-msg`, `.v2-msg-me`, `.v2-assist-input` | справа, 420 px, непрозрачная |
| заголовок и текст карточки | `.v2-card-title`, `.v2-text` | Unbounded 700 18 px; текст `--v2-text-2` |

## 6. Каркас

| часть | класс | правило |
|---|---|---|
| боковое меню | `.v2-side`, `.v2-side-item`, `.v2-side-label`, `.v2-side-section`, `.v2-side-recent`, `.v2-soon`, `.v2-side-foot` | 248 px; пункты: Главная, «Инструменты» (всплывающий `.v2-menu` по наведению и по клику: иконка в цвете, название, подпись), под ним 2–3 последних инструмента, раздел «Сообщество» (Новости, Обсуждения с меткой «скоро»), внизу «Свернуть» |
| полоса иконок | `.v2-side.is-rail` | 72 px, подписи, разделы и последние прячутся; состояние запоминается (письмо 2) |
| состояния пункта | `.is-active`, `.is-soon`, наведение, фокус | активный — `--v2-surface-hover` и белый текст; «скоро» — приглушённый, без действия |
| верхняя строка | `.v2-top`, `.v2-top-search`, `.v2-top-burger`, `.v2-avatar` | 64 px: поиск и аватар; кнопка меню только на телефоне |
| меню аватара | `.v2-menu` | Профиль, Админ-панель (только администратору; «Расход» внутри неё), Выйти. В боковом меню профиля и расхода нет |

### 6.1. Каркас на страницах (письмо 2)

Открывает `templates/_header.html`, закрывает `templates/_page_end.html`
ПОСЛЕ подвала — подвал стоит в колонке содержимого, не под меню. Каркас
получает вошедший с подтверждённой почтой; у гостя и на страницах входа
шапка прежняя. `shell_off` в контексте роута — страница без каркаса
осознанно (витрина `/botamin`). Инструмент страницы (цвет, активный
пункт, «последние») выводится из адреса по списку `V2_TOOLS` (`main.py`).
Поведение — `static/v2-shell.js`; стили — раздел 6а `static/v2.css`.

| часть | правило |
|---|---|
| «Инструменты» | всплывающий `.v2-menu` по наведению (где наведение есть) и по клику; Esc закрывает и возвращает фокус; стрелки ходят по пунктам. Ниже 1024 px — раскрывается на месте |
| последние | до трёх, свежий сверху, `localStorage['v2-recent']`; пункты рисует сервер скрытыми, встроенный скрипт показывает нужные до первой отрисовки; хранилище недоступно — список пуст |
| полоса | «Свернуть», `localStorage['v2-rail']`, класс `v2-rail` на `<html>` ставится до отрисовки; в полосе у пункта подсказка `title` |
| меню аватара | Профиль, Админ-панель (`/admin/users`, только администратору), Выйти |
| модалки | центрируются по колонке содержимого: `.v2-shell` объявляет `--app-aside` |
| ниже 1024 px | меню поверх страницы за кнопкой в верхней строке; закрывается крестиком, тапом по подложке, Esc |

## 7. Телефон (390)

| компонент | поведение |
|---|---|
| боковое меню | за кнопкой `.v2-top-burger` в верхней строке, выезжает поверх; «Инструменты» — раскрывающийся список на месте |
| шапка страницы | одна колонка; кнопки действий под описанием, слева |
| вкладки | прокручиваются вбок, не переносятся |
| чипы | переносятся |
| органы | высота 44 (`pointer: coarse`) |
| карточка | поле 16 |
| панель ассистента | на весь экран |
| меню | ширина по содержимому, не шире экрана |

## 8. Анимации

| токен | значение | где |
|---|---|---|
| `--v2-dur-fast` | 150ms | наведение, фокус |
| `--v2-dur-base` | 200ms | переключение вкладок, меню |
| `--v2-dur-enter` | 400ms | появление, прогресс, кольцо загрузки |
| `--v2-ease-out` | cubic-bezier(0, 0, 0.2, 1) | всё, что появляется |
| `--v2-ease` | cubic-bezier(0.4, 0, 0.2, 1) | всё, что перемещается |

При «уменьшить движение» переходы v2 отключены, кольцо загрузки стоит,
нажатие не масштабирует. Звёзды статичны всегда.

## 9. Соответствие «старое → новое»

Для токенов `style.css`, которые письма 2–8 заменят. Старые токены
в этом письме НЕ удаляются. «≈» — значение меняется, и это решение
редизайна, а не опечатка.

| старый | новый | примечание |
|---|---|---|
| `--surface-0` | `--v2-bg` | ≈ `#0A0B0D` → `#0a0b12` |
| `--surface-1` | `--v2-surface-1` | ≈ плашка → белый 2 % |
| `--surface-2` | `--v2-surface-2` | ≈ плашка → белый 4 % |
| `--surface-3` | `--v2-surface-hover` | ≈ |
| `--surface-sunken` | `--v2-surface-field` | ≈ |
| `--border-strong` | `--v2-line-1` | .14 = .14 |
| `--border` | `--v2-line-2` | ≈ .08 → .10 |
| `--border-hairline` | `--v2-line-3` | ≈ .06 → .07 |
| `--border-contrast` | `--v2-line-strong` | .36 = .36 |
| `--text` · `--text-2` · `--text-faint` · `--text-strong` | `--v2-text` · `--v2-text-2` · `--v2-text-faint` · `--v2-text-strong` | = |
| `--success` · `--warning` · `--error` | `--v2-ok` · `--v2-warn` · `--v2-danger` | = |
| `--font` · `--font-mono` | `--v2-font` · `--v2-font-mono` | = |
| — | `--v2-font-display` | новое: Unbounded |
| `--text-caption` · `--text-mono` | `--v2-fs-xs` | 13; `--text-mono` (12) ниже минимума — поднимается |
| `--text-body-sm` · `--text-nav` | `--v2-fs-sm` | = |
| `--text-body` | `--v2-fs-md` | = |
| `--text-body-lg` · `--text-body-xl` | `--v2-fs-lg` | ≈ 17 → 18 у первого |
| `--text-h3` | `--v2-fs-xl` | ≈ |
| `--text-h2` | `--v2-fs-2xl` | ≈ |
| `--text-h1` · `--text-display` | `--v2-fs-3xl` | ≈ |
| `--space-1` … `--space-7` | `--v2-sp-1` … `--v2-sp-7` | = |
| `--space-8` (72) | `--v2-sp-8` (64) либо `--v2-sp-9` (80) | ≈ по месту; 72 нет в шкале глобального правила |
| `--space-9` · `--space-10` | `--v2-sp-9` | ≈ 96 и 128 — выше потолка шкалы |
| `--radius-sm` · `--radius-md` · `--radius-lg` · `--radius-xl` | `--v2-r-sm` · `--v2-r-md` · `--v2-r-lg` · `--v2-r-xl` | = |
| `999px` в страничных файлах | `--v2-r-pill` | прямое значение уходит в токен |
| `--shadow-sm` · `--shadow-md` · `--shadow-lg` | `--v2-shadow-pop` либо без тени | ≈ карточки v2 теней не имеют |
| `--accent-hh` · `--accent-nutrition` · `--accent-medkit` · `--accent-enshrouded` · `--accent-workout` | `--v2-hh` · `--v2-nutrition` · `--v2-medkit` · `--v2-enshrouded` · `--v2-workout` | = |
| `--tool-accent` | `--v2-tool` | = |
| `--tool-accent-dim` | `--v2-tool-dim` | ≈ 15 % → 16 %, выводится |
| `--tool-accent-hover` | цвет светлеет на 14 % в самом правиле | ≈ |
| `--tool-accent-ink` | `--v2-ink-on-tool` | ≈ §4.3 |
| `--accent-brand` · `--accent-brand-hover` | `--v2-hh` · `--v2-grad-a` | вне инструментов бренд — синий HH |
| `.theme-hh` … `.theme-medkit` | `.v2-tool-hh` … `.v2-tool-medkit` | класс на `<body>` |
| `--dur-fast` · `--dur-base` · `--dur-slow` | `--v2-dur-fast` · `--v2-dur-base` · `--v2-dur-enter` | = |
| `--ease` · `--ease-out` | `--v2-ease` · `--v2-ease-out` | = |

Токены `--landing-*` не заменяются: гостевая главная вне редизайна.

## 10. Решения, которые не пересматриваются

Записаны в BACKLOG №352 целиком: направление, правило цвета
инструмента, каркас, внутреннее устройство инструмента, телефон.

**СВЕТЛОЙ ТЕМЫ НЕТ.** Письмо плана требовало удалить её вместе
с переключателем; замер этого захода показал, что удалять нечего:
`html.light`, `static/theme.js` и переключатель сняты ещё в миграции
2026 года (`design-system.md` §11), ни одного признака светлой темы
в коде и в базе нет (`py measure_redesign.py`, раздел 6). Тёмная —
единственная.

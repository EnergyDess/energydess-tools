# Только для локального прогона тестов. В образ не уезжает: Dockerfile
# ставит requirements.txt, а лишние пакеты в проде — лишняя поверхность.
-r requirements.txt
pytest

# Нужен ТОЛЬКО разведке сканера лекарств (`check_medkit_scan.py`,
# BACKLOG №163): рисует DataMatrix, чтобы спросить у библиотеки дневника
# питания, читает ли она его. Проба работает и без него — печатает
# «картинка НЕ НАРИСОВАНА» и НЕ выдаёт это за «формат не читается».
ppf-datamatrix

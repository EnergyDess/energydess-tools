# Только для локального прогона тестов. В образ не уезжает: Dockerfile
# ставит requirements.txt, а лишние пакеты в проде — лишняя поверхность.
-r requirements.txt
pytest

# Нужен ТОЛЬКО разведке сканера лекарств (`check_medkit_scan.py`,
# BACKLOG №163): рисует DataMatrix, чтобы спросить у библиотеки дневника
# питания, читает ли она его. Проба работает и без него — печатает
# «картинка НЕ НАРИСОВАНА» и НЕ выдаёт это за «формат не читается».
ppf-datamatrix

# Браузер проб: им водят пробы ряда стенда и проверки вёрстки. Стоял
# только на машине разработчика — то есть ряд нельзя было поднять
# нигде больше; CI ставит его этой строкой (BACKLOG №357).
playwright

# Счётчик записи процесса у ОС: им `check_upload_guard.py` спрашивает,
# что увидел ДИСК, пока шёл отвергнутый запрос (BACKLOG №327). Стоял
# только на машине разработчика — в CI проба падала ModuleNotFoundError.
psutil

fastapi
uvicorn[standard]
httpx
python-dotenv
jinja2
beautifulsoup4
lxml
sqlalchemy
bcrypt
python-jose[cryptography]
cryptography
python-multipart
pypdf
python-docx
Pillow
# База часовых поясов IANA для zoneinfo (agent_slots.py считает встречи
# в Europe/Moscow). В python:3.12-slim и на Windows системной базы нет,
# и ZoneInfo падает с ZoneInfoNotFoundError прямо на импорте модуля.
tzdata
# Статический ffmpeg с libx264 для сжатия роликов главной (BACKLOG №325).
# В python:3.12-slim ffmpeg нет, а apt тянет сотни мегабайт; колесо — 29 МБ.
imageio-ffmpeg
